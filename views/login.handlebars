<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Login Portal</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h1>Login</h1>
      <ul class="error">
        {{#errors}}
            <li>{{msg}}</li>
        {{/errors}}
      </ul>


    <form action="/login" method="post">
      <div><input type="text" name="username" value="{{credInfo.username}}" placeholder="username" /></div>
      <div><input type="password" name="password" value="{{credInfo.password}}" placeholder="password" /></div>
      <button>Login</button>
    </form>

    <p><a href="/signup">Or Sign Up</a></p>

  </body>
</html>
